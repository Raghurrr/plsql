DECLARE
   CURSOR c1 IS
      SELECT ename, sal
      FROM emp;

   vename emp.ename%TYPE;
   vsal   emp.sal%TYPE;

BEGIN
   OPEN c1;

   LOOP
      FETCH c1 INTO vename, vsal;

      EXIT WHEN c1%NOTFOUND;

      DBMS_OUTPUT.PUT_LINE(vename || ' ' || vsal);

   END LOOP;

   CLOSE c1;
END;
/
